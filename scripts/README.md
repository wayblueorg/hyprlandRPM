# Scripts

These are not-particularly-tested works-on-my-machine build scripts,
to test builds before they're pushed to copr.

To use,
initialize ~/rpmbuild/ the normal way,
then customize the repo files with your username,
and copy them to `/etc/yum.repos.d/`.

